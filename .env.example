# Required for Railway (and local dev)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
CHEER_TIME=09:00

# Optional: directory for members.json (default: data)
# Railway Volume mount path should match this (e.g. /app/data)
# DATA_DIR=data
