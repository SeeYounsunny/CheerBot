[build]
builder = "NIXPACKS"

# Start via Procfile (worker: python app.py). Do not set startCommand here —
# it overrides the Procfile and can make Railway treat the service as web-only.
[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
